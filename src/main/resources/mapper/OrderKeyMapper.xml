<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beau.graduation.dao.OrderKeyDao">

    <resultMap id="BaseResultMap" type="com.beau.graduation.model.BookOrder" >
        <result column="id" property="id" />
        <result column="book_id" property="bookId" />
        <result column="user_id" property="userId" />
        <result column="amount" property="amount" />
        <result column="order_id" property="orderId" />
        <result column="confirm_status" property="confirmStatus" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="total_price" property="totalPrice" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <sql id="Base_Column_List">
        `id`,
        `book_id`,
        `user_id`,
        `amount`,
        `order_id`,
        `confirm_status`,
        `create_time`,
        `update_time`,
        `total_price`,
        `del_flag`
    </sql>

    <!-- 插入数据 -->
    <insert id="insert" parameterType="com.beau.graduation.model.BookOrder">
        INSERT INTO tm_order
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test ='null != bookOrder.id'>
                `id`,
            </if>
            <if test ='null != bookOrder.bookId'>
                `book_id`,
            </if>
            <if test ='null != bookOrder.userId'>
                `user_id`,
            </if>
            <if test ='null != bookOrder.amount'>
                `amount`,
            </if>
            <if test ='null != bookOrder.orderId'>
                `order_id`,
            </if>
            <if test ='null != bookOrder.confirmStatus'>
                `confirm_status`,
            </if>
            <if test ='null != bookOrder.createTime'>
                `create_time`,
            </if>
            <if test ='null != bookOrder.updateTime'>
                `update_time`,
            </if>
            <if test ='null != bookOrder.totalPrice'>
                `total_price`,
            </if>
            <if test ='null != bookOrder.delFlag'>
                `del_flag`
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test ='null != bookOrder.id'>
                #{bookOrder.id},
            </if>
            <if test ='null != bookOrder.bookId'>
                #{bookOrder.bookId},
            </if>
            <if test ='null != bookOrder.userId'>
                #{bookOrder.userId},
            </if>
            <if test ='null != bookOrder.amount'>
                #{bookOrder.amount},
            </if>
            <if test ='null != bookOrder.orderId'>
                #{bookOrder.orderId},
            </if>
            <if test ='null != bookOrder.confirmStatus'>
                #{bookOrder.confirmStatus},
            </if>
            <if test ='null != bookOrder.createTime'>
                #{bookOrder.createTime},
            </if>
            <if test ='null != bookOrder.updateTime'>
                #{bookOrder.updateTime},
            </if>
            <if test ='null != bookOrder.totalPrice'>
                #{bookOrder.totalPrice},
            </if>
            <if test ='null != bookOrder.delFlag'>
                #{bookOrder.delFlag}
            </if>
        </trim>
    </insert>

    <!-- 批量插入数据 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO tm_order ( <include refid="Base_Column_List" /> ) VALUES
        <foreach collection="list" item="curr" index="index" separator=",">
            (
                #{curr.id},
                #{curr.bookId},
                #{curr.userId},
                #{curr.amount},
                #{curr.orderId},
                #{curr.confirmStatus},
                #{curr.createTime},
                #{curr.updateTime},
                #{curr.totalPrice},
                #{curr.delFlag}
            )
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.beau.graduation.model.BookOrder">
        UPDATE tm_order
        <set>
            <if test ='null != bookOrder.bookId'>book_id = #{bookOrder.bookId},</if>
            <if test ='null != bookOrder.userId'>user_id = #{bookOrder.userId},</if>
            <if test ='null != bookOrder.amount'>amount = #{bookOrder.amount},</if>
            <if test ='null != bookOrder.orderId'>order_id = #{bookOrder.orderId},</if>
            <if test ='null != bookOrder.confirmStatus'>confirm_status = #{bookOrder.confirmStatus},</if>
            <if test ='null != bookOrder.createTime'>create_time = #{bookOrder.createTime},</if>
            <if test ='null != bookOrder.updateTime'>update_time = #{bookOrder.updateTime},</if>
            <if test ='null != bookOrder.totalPrice'>total_price = #{bookOrder.totalPrice},</if>
            <if test ='null != bookOrder.delFlag'>del_flag = #{bookOrder.delFlag}</if>
        </set>
        WHERE id = bookOrder.id
    </update>

    <!-- 删除 -->
    <delete id="delete">
        DELETE FROM tm_order
        WHERE id = #{bookOrder.bookOrder}
    </delete>

    <!-- 批量删除 -->
    <delete id="batchDelete" parameterType = "java.util.List">
        DELETE FROM tm_order WHERE id IN
        <foreach collection="list"  item="item" open="(" separator="," close=")"  >
            #{item}
        </foreach>
    </delete>

    <!-- 主键查询 -->
    <select id="selectByObj" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM tm_order
        WHERE id = #{bookOrder.id}
    </select>

    <!-- 条件查询 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM tm_order
        <where>
            <if test ='null != bookOrder.id'>
                and id = #{bookOrder.id}
            </if>
            <if test ='null != bookOrder.bookId'>
                and bookId = #{bookOrder.bookId}
            </if>
            <if test ='null != bookOrder.userId'>
                and userId = #{bookOrder.userId}
            </if>
            <if test ='null != bookOrder.amount'>
                and amount = #{bookOrder.amount}
            </if>
            <if test ='null != bookOrder.orderId'>
                and orderId = #{bookOrder.orderId}
            </if>
            <if test ='null != bookOrder.confirmStatus'>
                and confirmStatus = #{bookOrder.confirmStatus}
            </if>
            <if test ='null != bookOrder.createTime'>
                and createTime = #{bookOrder.createTime}
            </if>
            <if test ='null != bookOrder.updateTime'>
                and updateTime = #{bookOrder.updateTime}
            </if>
            <if test ='null != bookOrder.totalPrice'>
                and totalPrice = #{bookOrder.totalPrice}
            </if>
            <if test ='null != bookOrder.delFlag'>
                and delFlag = #{bookOrder.delFlag}
            </if>
        </where>
    </select>

    <!-- 分页条件查询 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM tm_order
        <where>
            <if test ='null != bookOrder.id'>
                and id = #{bookOrder.id}
            </if>
            <if test ='null != bookOrder.bookId'>
                and bookId = #{bookOrder.bookId}
            </if>
            <if test ='null != bookOrder.userId'>
                and userId = #{bookOrder.userId}
            </if>
            <if test ='null != bookOrder.amount'>
                and amount = #{bookOrder.amount}
            </if>
            <if test ='null != bookOrder.orderId'>
                and orderId = #{bookOrder.orderId}
            </if>
            <if test ='null != bookOrder.confirmStatus'>
                and confirmStatus = #{bookOrder.confirmStatus}
            </if>
            <if test ='null != bookOrder.createTime'>
                and createTime = #{bookOrder.createTime}
            </if>
            <if test ='null != bookOrder.updateTime'>
                and updateTime = #{bookOrder.updateTime}
            </if>
            <if test ='null != bookOrder.totalPrice'>
                and totalPrice = #{bookOrder.totalPrice}
            </if>
            <if test ='null != bookOrder.delFlag'>
                and delFlag = #{bookOrder.delFlag}
            </if>
        </where>
        limit #{page,jdbcType=INTEGER}, #{pageSize,jdbcType=INTEGER}
    </select>

    <!-- 总量查询 -->
    <select id="total" resultType="java.lang.Integer">
        SELECT count(id) FROM tm_order
        <where>
            <if test ='null != bookOrder.id'>
                and id = #{bookOrder.id}
            </if>
            <if test ='null != bookOrder.bookId'>
                and bookId = #{bookOrder.bookId}
            </if>
            <if test ='null != bookOrder.userId'>
                and userId = #{bookOrder.userId}
            </if>
            <if test ='null != bookOrder.amount'>
                and amount = #{bookOrder.amount}
            </if>
            <if test ='null != bookOrder.orderId'>
                and orderId = #{bookOrder.orderId}
            </if>
            <if test ='null != bookOrder.confirmStatus'>
                and confirmStatus = #{bookOrder.confirmStatus}
            </if>
            <if test ='null != bookOrder.createTime'>
                and createTime = #{bookOrder.createTime}
            </if>
            <if test ='null != bookOrder.updateTime'>
                and updateTime = #{bookOrder.updateTime}
            </if>
            <if test ='null != bookOrder.totalPrice'>
                and totalPrice = #{bookOrder.totalPrice}
            </if>
            <if test ='null != bookOrder.delFlag'>
                and delFlag = #{bookOrder.delFlag}
            </if>
        </where>
    </select>
</mapper>